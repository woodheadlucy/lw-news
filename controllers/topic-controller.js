

// e.g

// fetchTopic().then(([topic])=>{
// if undefined then reject
//     if (!topic) return Promise.reject({status: 404, message: 'topic not found'})
//     //otherwise respond as normal
//     res.status(200).send({topic})
// }).catch(next)}
